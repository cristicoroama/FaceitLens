# Python
__pycache__/
*.pyc
.venv/
venv/
db.sqlite3
staticfiles/

# Node
node_modules/
dist/

# Env / secrets
.env
*.env

# Local tool settings — machine-specific paths, not useful to anyone else.
# The whole directory, not just the one file: gitignore has no effect on
# already-tracked paths, which is why settings.local.json stayed in the repo
# despite being listed here.
.claude/
